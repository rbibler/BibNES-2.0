package com.bibler.awesome.bibnes20.controllers;

//import net.java.games.input.Controller;
//import net.java.games.input.ControllerEnvironment;

public class JInputControllerManager {
	/*
	public static void listControllers() {
		//Controller[] ca = ControllerEnvironment.getDefaultEnvironment().getControllers();

		for(int i =0;i<ca.length;i++){

        
			System.out.println(ca[i].getName());
		}
	}
	
	public static USBGamepad getFirstUSBPad() {
		Controller[] ca = ControllerEnvironment.getDefaultEnvironment().getControllers();
		for(Controller controller : ca) {
			if(controller.getName().contains("USB")) {
				System.out.println("Found usb");
				USBGamepad g = new USBGamepad(controller);
				return g;
			}
		}
		return null;
	}
*/
}
